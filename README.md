# README

* To run server:
`$ bundle exec rails s`

* To run tests:
`$ bundle exec rspec`

* Run in docker:
* Buid:
  `$ docker build -t search-hub:latest .`

* Run:
  `$ docker run -p 3000:3000 -it search-hub:latest`

